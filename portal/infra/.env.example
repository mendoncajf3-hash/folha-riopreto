# Variáveis do docker-compose (produção). Copie para infra/.env e ajuste.
POSTGRES_USER=portal
POSTGRES_PASSWORD=troque-esta-senha
POSTGRES_DB=portal
# Gere com: openssl rand -hex 32
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
# Domínio público do portal
WEB_ORIGIN=http://localhost
HTTP_PORT=80
