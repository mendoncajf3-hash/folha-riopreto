{
  "name": "portal-operacional",
  "version": "0.1.0",
  "private": true,
  "description": "Portal de Gestão Operacional — monólito modular (API + Web)",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "api": "pnpm --filter @portal/api",
    "web": "pnpm --filter @portal/web"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.0"
}
